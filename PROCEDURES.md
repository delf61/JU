# Complete Procedure Inventory

This file documents all `Pp*` (Procedure/Macro) objects identified in JU.RDB.

## PpPrijem

- **Procedure name**: `PpPrijem`
- **Purpose**: Handles incoming transactions or cash (Prijem) (INFERRED from name).
- **Usage**: The string `prijem` appears `229` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVydaj

- **Procedure name**: `PpVydaj`
- **Purpose**: Handles outgoing transactions or cash (Vydaj) (INFERRED from name).
- **Usage**: The string `vydaj` appears `677` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDat1

- **Procedure name**: `PpDat1`
- **Purpose**: Handles date processing or calendar (INFERRED from name).
- **Usage**: The string `dat1` appears `85` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDat2

- **Procedure name**: `PpDat2`
- **Purpose**: Handles date processing or calendar (INFERRED from name).
- **Usage**: The string `dat2` appears `86` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDoklady

- **Procedure name**: `PpDoklady`
- **Purpose**: Handles document management (Doklady) (INFERRED from name).
- **Usage**: The string `doklady` appears `36` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpOkno

- **Procedure name**: `PpOkno`
- **Purpose**: UI window management (Okno) (INFERRED from name).
- **Usage**: The string `okno` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSadzbDPH

- **Procedure name**: `PpSadzbDPH`
- **Purpose**: VAT/DPH processing (INFERRED from name).
- **Usage**: The string `sadzbdph` appears `32` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpTrasy

- **Procedure name**: `PpTrasy`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `trasy` appears `129` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpStratyDoch

- **Procedure name**: `PpStratyDoch`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `stratydoch` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpIKzp

- **Procedure name**: `PpIKzp`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `ikzp` appears `141` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpIKdkp

- **Procedure name**: `PpIKdkp`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `ikdkp` appears `61` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDohoda

- **Procedure name**: `PpDohoda`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `dohoda` appears `25` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpHlaSklad

- **Procedure name**: `PpHlaSklad`
- **Purpose**: Inventory/Warehouse processing (Sklad) (INFERRED from name).
- **Usage**: The string `hlasklad` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSklad

- **Procedure name**: `PpSklad`
- **Purpose**: Inventory/Warehouse processing (Sklad) (INFERRED from name).
- **Usage**: The string `sklad` appears `134` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSklad2008

- **Procedure name**: `PpSklad2008`
- **Purpose**: Inventory/Warehouse processing (Sklad) (INFERRED from name).
- **Usage**: The string `sklad2008` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpUhrady_All

- **Procedure name**: `PpUhrady_All`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `uhrady_all` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSC_Cat

- **Procedure name**: `PpSC_Cat`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `sc_cat` appears `0` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSC

- **Procedure name**: `PpSC`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `sc` appears `642` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSC_nova

- **Procedure name**: `PpSC_nova`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `sc_nova` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpGraf_PD

- **Procedure name**: `PpGraf_PD`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `graf_pd` appears `6` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpStatist

- **Procedure name**: `PpStatist`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `statist` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSpotreba

- **Procedure name**: `PpSpotreba`
- **Purpose**: Vehicle fuel consumption tracking (Spotreba) (INFERRED from name).
- **Usage**: The string `spotreba` appears `355` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSpotre_SC

- **Procedure name**: `PpSpotre_SC`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `spotre_sc` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSpotrSCsum

- **Procedure name**: `PpSpotrSCsum`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `spotrscsum` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSpotreba_n

- **Procedure name**: `PpSpotreba_n`
- **Purpose**: Vehicle fuel consumption tracking (Spotreba) (INFERRED from name).
- **Usage**: The string `spotreba_n` appears `38` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpEvi_Auto

- **Procedure name**: `PpEvi_Auto`
- **Purpose**: Vehicle tracking (Auto) (INFERRED from name).
- **Usage**: The string `evi_auto` appears `194` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKm_Kon_Vzd

- **Procedure name**: `PpKm_Kon_Vzd`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `km_kon_vzd` appears `9` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDovod_SC

- **Procedure name**: `PpDovod_SC`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `dovod_sc` appears `47` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpTrasa

- **Procedure name**: `PpTrasa`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `trasa` appears `38` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpHlaTra

- **Procedure name**: `PpHlaTra`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `hlatra` appears `8` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKraje

- **Procedure name**: `PpKraje`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `kraje` appears `27` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpOkresy

- **Procedure name**: `PpOkresy`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `okresy` appears `27` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpMesta

- **Procedure name**: `PpMesta`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `mesta` appears `70` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpMesto_SC

- **Procedure name**: `PpMesto_SC`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `mesto_sc` appears `0` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpBanky

- **Procedure name**: `PpBanky`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `banky` appears `44` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpHlaMes

- **Procedure name**: `PpHlaMes`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `hlames` appears `12` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpAuto

- **Procedure name**: `PpAuto`
- **Purpose**: Vehicle tracking (Auto) (INFERRED from name).
- **Usage**: The string `auto` appears `474` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDoprPros

- **Procedure name**: `PpDoprPros`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `doprpros` appears `27` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpAuto_Info

- **Procedure name**: `PpAuto_Info`
- **Purpose**: Vehicle tracking (Auto) (INFERRED from name).
- **Usage**: The string `auto_info` appears `11` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpAuto_new

- **Procedure name**: `PpAuto_new`
- **Purpose**: Vehicle tracking (Auto) (INFERRED from name).
- **Usage**: The string `auto_new` appears `0` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSC_EviAuto

- **Procedure name**: `PpSC_EviAuto`
- **Purpose**: Vehicle tracking (Auto) (INFERRED from name).
- **Usage**: The string `sc_eviauto` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpCeda

- **Procedure name**: `PpCeda`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `ceda` appears `5` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVitajte

- **Procedure name**: `PpVitajte`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vitajte` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpUdaje

- **Procedure name**: `PpUdaje`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `udaje` appears `172` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpTlf

- **Procedure name**: `PpTlf`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `tlf` appears `59` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpCisloOP

- **Procedure name**: `PpCisloOP`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `cisloop` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpTlf_odber

- **Procedure name**: `PpTlf_odber`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `tlf_odber` appears `12` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDopln_Uda

- **Procedure name**: `PpDopln_Uda`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `dopln_uda` appears `5` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpHladaj

- **Procedure name**: `PpHladaj`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `hladaj` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPV

- **Procedure name**: `PpPV`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pv` appears `140` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDElf

- **Procedure name**: `PpDElf`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `delf` appears `172` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDPH

- **Procedure name**: `PpDPH`
- **Purpose**: VAT/DPH processing (INFERRED from name).
- **Usage**: The string `dph` appears `1438` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVydaje

- **Procedure name**: `PpVydaje`
- **Purpose**: Handles outgoing transactions or cash (Vydaj) (INFERRED from name).
- **Usage**: The string `vydaje` appears `244` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpZmenKodVyd

- **Procedure name**: `PpZmenKodVyd`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `zmenkodvyd` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPrijmy

- **Procedure name**: `PpPrijmy`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `prijmy` appears `75` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpZmenKodPri

- **Procedure name**: `PpZmenKodPri`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `zmenkodpri` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDdatum

- **Procedure name**: `PpDdatum`
- **Purpose**: Handles date processing or calendar (INFERRED from name).
- **Usage**: The string `ddatum` appears `0` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVydaje_Kod

- **Procedure name**: `PpVydaje_Kod`
- **Purpose**: Handles outgoing transactions or cash (Vydaj) (INFERRED from name).
- **Usage**: The string `vydaje_kod` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPrijmy_Kod

- **Procedure name**: `PpPrijmy_Kod`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `prijmy_kod` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVyd_Info

- **Procedure name**: `PpVyd_Info`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vyd_info` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPri_Info

- **Procedure name**: `PpPri_Info`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pri_info` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPD

- **Procedure name**: `PpPD`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pd` appears `1249` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPD_banka

- **Procedure name**: `PpPD_banka`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pd_banka` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpStlacEnd

- **Procedure name**: `PpStlacEnd`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `stlacend` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPDkod

- **Procedure name**: `PpPDkod`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pdkod` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPD_Doklad

- **Procedure name**: `PpPD_Doklad`
- **Purpose**: Handles document management (Doklady) (INFERRED from name).
- **Usage**: The string `pd_doklad` appears `18` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPD_Info

- **Procedure name**: `PpPD_Info`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pd_info` appears `21` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpHla_PD

- **Procedure name**: `PpHla_PD`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `hla_pd` appears `26` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpHm_a_Nehm

- **Procedure name**: `PpHm_a_Nehm`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `hm_a_nehm` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpNaklady

- **Procedure name**: `PpNaklady`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `naklady` appears `7` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpLeasing

- **Procedure name**: `PpLeasing`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `leasing` appears `26` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPohladavky

- **Procedure name**: `PpPohladavky`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pohladavky` appears `11` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpUhra_KP

- **Procedure name**: `PpUhra_KP`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `uhra_kp` appears `7` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPohla_SC

- **Procedure name**: `PpPohla_SC`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pohla_sc` appears `0` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpZmenKP_AB

- **Procedure name**: `PpZmenKP_AB`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `zmenkp_ab` appears `6` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpCislo_REKL

- **Procedure name**: `PpCislo_REKL`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `cislo_rekl` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKPpol

- **Procedure name**: `PpKPpol`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `kppol` appears `79` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKPpol_rekl

- **Procedure name**: `PpKPpol_rekl`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `kppol_rekl` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSklad_rekl

- **Procedure name**: `PpSklad_rekl`
- **Purpose**: Inventory/Warehouse processing (Sklad) (INFERRED from name).
- **Usage**: The string `sklad_rekl` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpOP

- **Procedure name**: `PpOP`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `op` appears `1594` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPomRataj

- **Procedure name**: `PpPomRataj`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pomrataj` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPohl_Spolu

- **Procedure name**: `PpPohl_Spolu`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pohl_spolu` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpCislo_KP

- **Procedure name**: `PpCislo_KP`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `cislo_kp` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDovod_KP

- **Procedure name**: `PpDovod_KP`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `dovod_kp` appears `8` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpZavazky

- **Procedure name**: `PpZavazky`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `zavazky` appears `5` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpReklamacie

- **Procedure name**: `PpReklamacie`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `reklamacie` appears `5` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpUhra_KZ

- **Procedure name**: `PpUhra_KZ`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `uhra_kz` appears `6` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKZ_sc

- **Procedure name**: `PpKZ_sc`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `kz_sc` appears `8` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpREKL_sc

- **Procedure name**: `PpREKL_sc`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `rekl_sc` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKP_sc

- **Procedure name**: `PpKP_sc`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `kp_sc` appears `6` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVyberDod

- **Procedure name**: `PpVyberDod`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vyberdod` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVyberOdb

- **Procedure name**: `PpVyberOdb`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vyberodb` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpZmenKZ_AB

- **Procedure name**: `PpZmenKZ_AB`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `zmenkz_ab` appears `10` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKzPol

- **Procedure name**: `PpKzPol`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `kzpol` appears `100` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpREKLPol

- **Procedure name**: `PpREKLPol`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `reklpol` appears `39` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpUp

- **Procedure name**: `PpUp`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `up` appears `585` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSumPrikUhr

- **Procedure name**: `PpSumPrikUhr`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `sumprikuhr` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpCislo_KZ

- **Procedure name**: `PpCislo_KZ`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `cislo_kz` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDovod_KZ

- **Procedure name**: `PpDovod_KZ`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `dovod_kz` appears `10` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpEviZakazky

- **Procedure name**: `PpEviZakazky`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `evizakazky` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpZak_spolu

- **Procedure name**: `PpZak_spolu`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `zak_spolu` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpCislo_EZ

- **Procedure name**: `PpCislo_EZ`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `cislo_ez` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKalendar

- **Procedure name**: `PpKalendar`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `kalendar` appears `96` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKalendar_M

- **Procedure name**: `PpKalendar_M`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `kalendar_m` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpBeznyUcet

- **Procedure name**: `PpBeznyUcet`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `beznyucet` appears `6` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpUcet_Spolu

- **Procedure name**: `PpUcet_Spolu`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `ucet_spolu` appears `5` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpEviPrace

- **Procedure name**: `PpEviPrace`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `eviprace` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPrace2sc

- **Procedure name**: `PpPrace2sc`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `prace2sc` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpNovaTrasa

- **Procedure name**: `PpNovaTrasa`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `novatrasa` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVyberTrasu

- **Procedure name**: `PpVyberTrasu`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vybertrasu` appears `8` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPracePom

- **Procedure name**: `PpPracePom`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pracepom` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPracePom1

- **Procedure name**: `PpPracePom1`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pracepom1` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPoklDokl

- **Procedure name**: `PpPoklDokl`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pokldokl` appears `33` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPokl_Spolu

- **Procedure name**: `PpPokl_Spolu`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pokl_spolu` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpIKZP_cislo

- **Procedure name**: `PpIKZP_cislo`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `ikzp_cislo` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVydajDKP

- **Procedure name**: `PpVydajDKP`
- **Purpose**: Handles outgoing transactions or cash (Vydaj) (INFERRED from name).
- **Usage**: The string `vydajdkp` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVydajZP

- **Procedure name**: `PpVydajZP`
- **Purpose**: Handles outgoing transactions or cash (Vydaj) (INFERRED from name).
- **Usage**: The string `vydajzp` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPDprerus_B

- **Procedure name**: `PpPDprerus_B`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pdprerus_b` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVypisCislo

- **Procedure name**: `PpVypisCislo`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vypiscislo` appears `8` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPDprerus_V

- **Procedure name**: `PpPDprerus_V`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pdprerus_v` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPDprerus_P

- **Procedure name**: `PpPDprerus_P`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pdprerus_p` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPDsuma

- **Procedure name**: `PpPDsuma`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `pdsuma` appears `20` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpHladac

- **Procedure name**: `PpHladac`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `hladac` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpHla_1

- **Procedure name**: `PpHla_1`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `hla_1` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpCtrlF2

- **Procedure name**: `PpCtrlF2`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `ctrlf2` appears `7` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPrazdna

- **Procedure name**: `PpPrazdna`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `prazdna` appears `159` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpTlacDokl

- **Procedure name**: `PpTlacDokl`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `tlacdokl` appears `27` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpEnter

- **Procedure name**: `PpEnter`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `enter` appears `13` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpEnter_5

- **Procedure name**: `PpEnter_5`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `enter_5` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSpRia

- **Procedure name**: `PpSpRia`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `spria` appears `21` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVerzia

- **Procedure name**: `PpVerzia`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `verzia` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSet

- **Procedure name**: `PpSet`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `set` appears `423` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpMemDisk

- **Procedure name**: `PpMemDisk`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `memdisk` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpCatS

- **Procedure name**: `PpCatS`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `cats` appears `56` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVytvorCat

- **Procedure name**: `PpVytvorCat`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vytvorcat` appears `6` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpCatalog

- **Procedure name**: `PpCatalog`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `catalog` appears `263` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpZalohuj

- **Procedure name**: `PpZalohuj`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `zalohuj` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpUvod

- **Procedure name**: `PpUvod`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `uvod` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpHlavneMenu

- **Procedure name**: `PpHlavneMenu`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `hlavnemenu` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVseobData

- **Procedure name**: `PpVseobData`
- **Purpose**: Handles date processing or calendar (INFERRED from name).
- **Usage**: The string `vseobdata` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## Pprevolv

- **Procedure name**: `Pprevolv`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `revolv` appears `6` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpBytUdaje

- **Procedure name**: `PpBytUdaje`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `bytudaje` appears `12` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDomUdaje

- **Procedure name**: `PpDomUdaje`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `domudaje` appears `5` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpBytDetail

- **Procedure name**: `PpBytDetail`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `bytdetail` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDomacnost

- **Procedure name**: `PpDomacnost`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `domacnost` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVyuctSBD

- **Procedure name**: `PpVyuctSBD`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vyuctsbd` appears `5` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPoistky

- **Procedure name**: `PpPoistky`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `poistky` appears `10` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpNoveVyuSSE

- **Procedure name**: `PpNoveVyuSSE`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `novevyusse` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVyuctSSE

- **Procedure name**: `PpVyuctSSE`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vyuctsse` appears `26` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpBaterie

- **Procedure name**: `PpBaterie`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `baterie` appears `26` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpNoveTeplo

- **Procedure name**: `PpNoveTeplo`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `noveteplo` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpNoveVyuSPP

- **Procedure name**: `PpNoveVyuSPP`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `novevyuspp` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVyuctSPP

- **Procedure name**: `PpVyuctSPP`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vyuctspp` appears `20` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpInkaso

- **Procedure name**: `PpInkaso`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `inkaso` appears `39` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpPlatby_BU

- **Procedure name**: `PpPlatby_BU`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `platby_bu` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDruhDruh

- **Procedure name**: `PpDruhDruh`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `druhdruh` appears `21` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKodObchodu

- **Procedure name**: `PpKodObchodu`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `kodobchodu` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVyberMesto

- **Procedure name**: `PpVyberMesto`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vybermesto` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpHla_t

- **Procedure name**: `PpHla_t`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `hla_t` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpNovyNakup

- **Procedure name**: `PpNovyNakup`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `novynakup` appears `0` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpDruhTovaru

- **Procedure name**: `PpDruhTovaru`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `druhtovaru` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpNakup_o

- **Procedure name**: `PpNakup_o`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `nakup_o` appears `49` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpTlac

- **Procedure name**: `PpTlac`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `tlac` appears `42` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpZmenObchod

- **Procedure name**: `PpZmenObchod`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `zmenobchod` appears `2` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpSumNakup

- **Procedure name**: `PpSumNakup`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `sumnakup` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpNakup_T

- **Procedure name**: `PpNakup_T`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `nakup_t` appears `34` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpZmenTovar

- **Procedure name**: `PpZmenTovar`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `zmentovar` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpNovyTovar

- **Procedure name**: `PpNovyTovar`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `novytovar` appears `3` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpKodTovaru

- **Procedure name**: `PpKodTovaru`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `kodtovaru` appears `1` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpVystav

- **Procedure name**: `PpVystav`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `vystav` appears `10` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## PpZav2003

- **Procedure name**: `PpZav2003`
- **Purpose**: UNKNOWN (INFERRED from name).
- **Usage**: The string `zav2003` appears `4` times in the source (FACT).
- **Detailed logic (tables read/written, variables, invoked forms)**: UNKNOWN (Due to the proprietary binary structure of TTT and lack of explicit block delimiters, the exact FAND statements inside this procedure cannot be reliably isolated).

## Migration relevance

This inventory maps out the scope of the legacy MS-DOS application. Because the exact business logic and table schemas are locked behind PC FAND's binary pointer format, a specialized PC FAND decompiler is necessary to extract the final `CREATE TABLE` and raw procedural logic to accurately write CodeIgniter 4 controllers and MariaDB schemas. What is documented here provides the architectural blueprint of what objects exist and their inferred purposes.
