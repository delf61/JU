# Complete Form Inventory

This file documents all `Ee*` (Form/Screen) objects identified in JU.RDB.

## Ee

- **Form name**: `Ee`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeParDat

- **Form name**: `EeParDat`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeParDat1

- **Form name**: `EeParDat1`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeParDat2

- **Form name**: `EeParDat2`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePrijem

- **Form name**: `EePrijem`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeVydaj

- **Form name**: `EeVydaj`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKalendar

- **Form name**: `EeKalendar`
- **Associated table**: `FKalendar` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKalendar_1

- **Form name**: `EeKalendar_1`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeDat1

- **Form name**: `EeDat1`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeDat2

- **Form name**: `EeDat2`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeDoklady

- **Form name**: `EeDoklady`
- **Associated table**: `FDoklady` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSadzbDPH

- **Form name**: `EeSadzbDPH`
- **Associated table**: `FSadzbDPH` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeBanky

- **Form name**: `EeBanky`
- **Associated table**: `FBanky` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeMesto

- **Form name**: `EeMesto`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeMesta

- **Form name**: `EeMesta`
- **Associated table**: `FMesta` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeTrasy

- **Form name**: `EeTrasy`
- **Associated table**: `FTrasy` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUdaje

- **Form name**: `EeUdaje`
- **Associated table**: `FUdaje` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePrijmy

- **Form name**: `EePrijmy`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePrijmz

- **Form name**: `EePrijmz`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePrijem_PD

- **Form name**: `EePrijem_PD`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeVydajd

- **Form name**: `EeVydajd`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeVydaj_PD

- **Form name**: `EeVydaj_PD`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeVydajC

- **Form name**: `EeVydajC`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUcty

- **Form name**: `EeUcty`
- **Associated table**: `FUcty` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUcet

- **Form name**: `EeUcet`
- **Associated table**: `FUcet` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUcet_new

- **Form name**: `EeUcet_new`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUcet_bro

- **Form name**: `EeUcet_bro`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePV

- **Form name**: `EePV`
- **Associated table**: `FPV` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeStrataDoch

- **Form name**: `EeStrataDoch`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSumaPD

- **Form name**: `EeSumaPD`
- **Associated table**: `FSumaPD` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePD

- **Form name**: `EePD`
- **Associated table**: `FPD` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePDbrowse

- **Form name**: `EePDbrowse`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePDbrow_Vyd

- **Form name**: `EePDbrow_Vyd`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePDbrow_Pri

- **Form name**: `EePDbrow_Pri`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePDp

- **Form name**: `EePDp`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePDv

- **Form name**: `EePDv`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePDvyb

- **Form name**: `EePDvyb`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePDvklad

- **Form name**: `EePDvklad`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeIKzp

- **Form name**: `EeIKzp`
- **Associated table**: `FIKzp` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeIKzpBr

- **Form name**: `EeIKzpBr`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeIKdkp

- **Form name**: `EeIKdkp`
- **Associated table**: `FIKdkp` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeIKdkpBr

- **Form name**: `EeIKdkpBr`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeLeasing

- **Form name**: `EeLeasing`
- **Associated table**: `FLeasing` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeDohoda

- **Form name**: `EeDohoda`
- **Associated table**: `FDohoda` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeEZ

- **Form name**: `EeEZ`
- **Associated table**: `FEZ` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeEZ_browse

- **Form name**: `EeEZ_browse`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeDen_Prac

- **Form name**: `EeDen_Prac`
- **Associated table**: `FDen_Prac` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeDen_Prac_1

- **Form name**: `EeDen_Prac_1`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeDen_Prac_N

- **Form name**: `EeDen_Prac_N`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSkla2008

- **Form name**: `EeSkla2008`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSkl2008Br

- **Form name**: `EeSkl2008Br`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSklad

- **Form name**: `EeSklad`
- **Associated table**: `FSklad` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSkladBr

- **Form name**: `EeSkladBr`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSkladBrKP

- **Form name**: `EeSkladBrKP`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKPpol

- **Form name**: `EeKPpol`
- **Associated table**: `FKPpol` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKPpolBr

- **Form name**: `EeKPpolBr`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKP

- **Form name**: `EeKP`
- **Associated table**: `FKP` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKP_browse

- **Form name**: `EeKP_browse`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKZpol

- **Form name**: `EeKZpol`
- **Associated table**: `FKZpol` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeREKLpol

- **Form name**: `EeREKLpol`
- **Associated table**: `FREKLpol` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKZpol_br

- **Form name**: `EeKZpol_br`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeREpol_br

- **Form name**: `EeREpol_br`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKZ

- **Form name**: `EeKZ`
- **Associated table**: `FKZ` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeREKL

- **Form name**: `EeREKL`
- **Associated table**: `FREKL` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKZ_sta_new

- **Form name**: `EeKZ_sta_new`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeKZ_browse

- **Form name**: `EeKZ_browse`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUhrady

- **Form name**: `EeUhrady`
- **Associated table**: `FUhrady` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUhradyBr

- **Form name**: `EeUhradyBr`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeDoprPros

- **Form name**: `EeDoprPros`
- **Associated table**: `FDoprPros` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSC

- **Form name**: `EeSC`
- **Associated table**: `FSC` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSC_br

- **Form name**: `EeSC_br`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeAuto

- **Form name**: `EeAuto`
- **Associated table**: `FAuto` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeAutoUplne

- **Form name**: `EeAutoUplne`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSpotPrie

- **Form name**: `EeSpotPrie`
- **Associated table**: `FSpotPrie` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSpotreba

- **Form name**: `EeSpotreba`
- **Associated table**: `FSpotreba` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSpotrebaNm

- **Form name**: `EeSpotrebaNm`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSpotreba1

- **Form name**: `EeSpotreba1`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeSpotreba2

- **Form name**: `EeSpotreba2`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## Eesc_roky

- **Form name**: `Eesc_roky`
- **Associated table**: `Fsc_roky` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeEvi_Auto

- **Form name**: `EeEvi_Auto`
- **Associated table**: `FEvi_Auto` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeEvi_Auto_U

- **Form name**: `EeEvi_Auto_U`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUdajO

- **Form name**: `EeUdajO`
- **Associated table**: `FUdajO` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUdajF

- **Form name**: `EeUdajF`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUdajP

- **Form name**: `EeUdajP`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeUdajM

- **Form name**: `EeUdajM`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## Eedph

- **Form name**: `Eedph`
- **Associated table**: `Fdph` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeOP

- **Form name**: `EeOP`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePoklDokl

- **Form name**: `EePoklDokl`
- **Associated table**: `FPoklDokl` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## Eerevolv

- **Form name**: `Eerevolv`
- **Associated table**: `Frevolv` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeBytUdaje

- **Form name**: `EeBytUdaje`
- **Associated table**: `FBytUdaje` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeByt

- **Form name**: `EeByt`
- **Associated table**: `FByt` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeByt_A

- **Form name**: `EeByt_A`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeByt_B

- **Form name**: `EeByt_B`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePoistky

- **Form name**: `EePoistky`
- **Associated table**: `FPoistky` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePoistkyM

- **Form name**: `EePoistkyM`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePoistkyR

- **Form name**: `EePoistkyR`
- **Associated table**: UNKNOWN
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeVyuctSSE

- **Form name**: `EeVyuctSSE`
- **Associated table**: `FVyuctSSE` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeElSasa

- **Form name**: `EeElSasa`
- **Associated table**: `FElSasa` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeVyucVeol

- **Form name**: `EeVyucVeol`
- **Associated table**: `FVyucVeol` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeH2O_Sasa

- **Form name**: `EeH2O_Sasa`
- **Associated table**: `FH2O_Sasa` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeBaterie

- **Form name**: `EeBaterie`
- **Associated table**: `FBaterie` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeTeplo

- **Form name**: `EeTeplo`
- **Associated table**: `FTeplo` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeVyuctSPP

- **Form name**: `EeVyuctSPP`
- **Associated table**: `FVyuctSPP` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeInkaso

- **Form name**: `EeInkaso`
- **Associated table**: `FInkaso` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EePlatby

- **Form name**: `EePlatby`
- **Associated table**: `FPlatby` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeDruhDruh

- **Form name**: `EeDruhDruh`
- **Associated table**: `FDruhDruh` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeDruhTova

- **Form name**: `EeDruhTova`
- **Associated table**: `FDruhTova` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeObchody

- **Form name**: `EeObchody`
- **Associated table**: `FObchody` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeTovary

- **Form name**: `EeTovary`
- **Associated table**: `FTovary` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeNakup_o

- **Form name**: `EeNakup_o`
- **Associated table**: `FNakup_o` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## EeNakup_t

- **Form name**: `EeNakup_t`
- **Associated table**: `FNakup_t` (INFERRED based on standard PC FAND naming convention)
- **Detailed fields/layout**: UNKNOWN (Relies on `#K` layout markers which cannot be mapped to this specific form without structural offset mapping).

## Migration relevance

This inventory maps out the scope of the legacy MS-DOS application. Because the exact business logic and table schemas are locked behind PC FAND's binary pointer format, a specialized PC FAND decompiler is necessary to extract the final `CREATE TABLE` and raw procedural logic to accurately write CodeIgniter 4 controllers and MariaDB schemas. What is documented here provides the architectural blueprint of what objects exist and their inferred purposes.
